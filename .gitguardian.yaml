version: 2

secret:
  # Docker Compose and CI workflow files contain local development
  # credentials only (e.g., POSTGRES_PASSWORD: tsumugi) - not real secrets
  ignored_paths:
    - 'docker-compose.yml'
    - '.devcontainer/docker-compose.yml'
    - '.github/workflows/e2e-tests.yml'
